CIRCLE 利用枠表示・契約管理エラーの診断用修正

本番への反映は未完了です。Xクレジットの購入、利用枠の追加・復元、契約の解約は行っていません。

修正内容
- 分析とは別に「一覧取得の残り回数」を常に表示します。
- 月間の一覧取得枠を使い切った場合、対象の枠と実際の更新日を示します。
- 月間上限に対して架空の15分待ちを表示しません。
- Xが402を返した場合、運営側の補充待ちと表示します。
- 契約管理のAPIエラーを個別に識別し、秘密情報を含めずにStripeリクエストID等をサーバーログに記録します。

既存の月額・利用上限・同意・認証・解約手順は変更していません。X失敗時の回数消費も既存条件どおりです。

検証
25件成功。実際のSQLをSQLite上で実行し、Xの402失敗2回→一覧枠のみ上限→分析0回使用という症状を再現しました。
X停止中でもPortalは呼べること、権限エラー時に秘密情報をログに出さないこと、月間上限で15分待ち表示が出ないことを確認しました。
Netlifyの本番ビルド・実サービスのPortal API権限は未検証です。

反映先
monomod91-ops/xtools-Analyst の main、CIRCLE-Subscription-Update/CIRCLE-Netlify/ 以下。
このZIPはソースコードです。Netlifyの静的サイトのドロップ欄に直接アップロードしないでください。
既存Gitリポジトリのルートに、このZIP内のフォルダ階層を保って上書きし、7ファイルの差分を確認してコミットすると通常のGit連携ビルド対象になります。
本番元は commit 1586bb3123e8b5023c1087609e2bb8e0453a5a4f です。その後の編集がある場合は上書き前に差分を統合してください。

現在の運用上の確認点
- 月額1,980円の契約はStripe上でactive、支払済みを確認しました。
- X開発者クレジットが不足している旨は運営者の報告です。残高そのものにはアクセスできていません。補充は https://console.x.com/ から行います。
- 本番のSTRIPE_PORTAL_CONFIG_IDとStripe上の有効な設定は一致しています。
- 「契約・解約」失敗の直接原因はまだ未確認です。Stripe Workbenchで POST /v1/billing_portal/sessions の最新エラー本文を確認してください。
- Customer Portal の書き込み権限が必要ですが、現在の本番APIキーのその権限を直接確認する機能は接続ツールにありません。
